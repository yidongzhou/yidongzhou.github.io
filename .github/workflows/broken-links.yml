name: Check for broken links

on:
  push:
    branches:
      - master
      - main
    paths:
      - "assets/**"
      - "**.html"
      - "**.js"
      - "**.liquid"
      - "**/*.md"
      - "**.yml"
      - "!.github/workflows/axe.yml"
      - "!.github/workflows/deploy-docker-tag.yml"
      - "!.github/workflows/deploy-image.yml"
      - "!.github/workflows/docker-slim.yml"
      - "!.github/workflows/lighthouse-badger.yml"
      - "!.github/workflows/prettier.yml"
      - "!lighthouse_results/**"
  pull_request:
    branches:
      - master
      - main
    paths:
      - "assets/**"
      - "**.html"
      - "**.js"
      - "**.liquid"
      - "**/*.md"
      - "**.yml"
      - "!.github/workflows/axe.yml"
      - "!.github/workflows/deploy-docker-tag.yml"
      - "!.github/workflows/deploy-image.yml"
      - "!.github/workflows/docker-slim.yml"
      - "!.github/workflows/lighthouse-badger.yml"
      - "!.github/workflows/prettier.yml"
      - "!lighthouse_results/**"

jobs:
  link-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Link Checker 🔗
        uses: lycheeverse/lychee-action@v2.1.0
        with:
          fail: true
          # removed md files that include liquid tags
          args: --user-agent 'curl/7.54' --exclude-path README.md --exclude-path FAQ.md --exclude-path CUSTOMIZE.md --exclude-path INSTALL.md --exclude-path CONTRIBUTING.md --exclude-path _pages/404.md --exclude-path _pages/blog.md --exclude-path _posts/2018-12-22-distill.md --exclude-path _posts/2023-04-24-videos.md --exclude-path _posts/2015-07-15-code.md --exclude-path _posts/2015-10-20-math.md --exclude-path _posts/2023-05-12-custom-blockquotes.md --exclude-path _projects/3_project.md --exclude-path assets/pdf/STA100/Ch1.slides.html --exclude-path assets/pdf/STA100/Ch2.slides.html --exclude-path assets/pdf/STA100/Ch3.slides.html --exclude-path assets/pdf/STA100/Ch4.slides.html --exclude-path assets/pdf/STA100/Ch5.slides.html --exclude-path assets/pdf/STA100/Ch6.slides.html --exclude-path assets/pdf/STA100/Ch7.slides.html --exclude-path assets/pdf/STA100/Ch8.slides.html --exclude-path assets/pdf/STA100/Ch9.slides.html --exclude-path assets/pdf/STA100/Ch10.slides.html --exclude-path assets/pdf/STA100/Ch11.slides.html --exclude-path assets/pdf/STA100/Ch12.slides.html --verbose --no-progress './**/*.md' './**/*.html'
